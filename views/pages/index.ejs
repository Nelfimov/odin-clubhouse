<%- include('../partials/header') %>

<h1><%= title %></h1>
<% if (user) { %>
  <h2>Welcome back, <%= user.email %></h2>
<% } else { %>
  <h2>Please log in</h2>
<% } %>
<% if (locals.message) %>
  <p><%= locals.message %></p>

<%- include('../partials/footer') %>